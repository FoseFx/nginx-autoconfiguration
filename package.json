{
  "name": "nginx-autoconfig",
  "version": "1.0.0",
  "description": "Configures and restarts nginx on http requests",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "FoseFx",
  "license": "MIT",
  "dependencies": {
    "fs-extra": "^7.0.1"
  }
}
